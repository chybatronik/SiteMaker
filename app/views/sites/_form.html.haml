= simple_form_for(@site) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :description
    = f.input :type_site, collection:["blog"]
    = f.association :user

  .form-actions
    = f.button :submit
