%h1 Listing sites

%table.table
  %tr
    %th Name
    %th Description
    %th Type 
    %th Url
    %th User
    %th Show pages 
    %th
    %th
    %th

  - @sites.each do |site|
    %tr
      %td= site.name
      %td= site.description
      %td= site.type_site
      %td= link_to site.url_for_site, site.url_for_site
      %td= site.user
      %td= link_to "show pages", site_pages_path(site)
      %td= link_to 'Show', site
      %td= link_to 'Edit', edit_site_path(site)
      %td= link_to 'Destroy', site, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Site', new_site_path, class:"btn btn-default"
